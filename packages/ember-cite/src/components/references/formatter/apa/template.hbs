<ul ...attributes>
  {{#each @references as |r|}}
    {{#let (component (ensure-safe-component (concat 'references/formatter/apa/' r.type))) as |Ref|}}
      <Ref @reference={{r}} />
    {{/let}}
  {{/each}}
</ul>